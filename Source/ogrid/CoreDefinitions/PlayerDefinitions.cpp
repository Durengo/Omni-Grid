#include "PlayerDefinitions.h"

#include <stdexcept>

#include "GameDefinition/Player.h"
#include "GameDefinition/Piece.h"

namespace OGRID
{
    // std::vector<Player *> GetTicTacToePlayers(std::vector<MoveType *> &moveType)
    // {
    //     if (moveType.size() != 2)
    //     {
    //         throw std::invalid_argument("TicTacToe requires exactly 2 MoveTypes");
    //     }

    //     std::vector<Player *> players;

    //     players.push_back(new Player("Player 1", Human, moveType[0]));
    //     players.push_back(new Player("Player 2", Human, moveType[1]));

    //     return players;
    // }
}