#pragma once

#include <string>

struct GUIInfo{
    int width;
    int height;
    std::string windowName;
    int targetFPS;
    float gridSize;
    float cellSize;
    float lineThickness;
    float margin;
};
